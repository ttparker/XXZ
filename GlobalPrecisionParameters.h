#ifndef GPP_H
#define GPP_H

const int globalMinLancIters = 3,
          globalMaxLancIters = 100;
const double observableThreshold = 1e-11;
// if any observable's absolute value is smaller than this, then round it to zero

#endif
